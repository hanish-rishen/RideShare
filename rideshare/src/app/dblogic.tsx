import { supabase } from "@/lib/supabaseClient";

function addReq(start_loc: string, end_loc: string, user_id: string) {}

async function pair_users(
  start_loc: string,
  end_loc: string,
  user_id: string
) {}
